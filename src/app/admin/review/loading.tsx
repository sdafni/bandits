import { PageSkeleton } from "@/components/page-skeleton";

export default function AdminReviewLoading() {
  return <PageSkeleton subtitle="Preparing analyst queue and review cases." title="Loading review desk" />;
}
