import { PageSkeleton } from "@/components/page-skeleton";

export default function Loading() {
  return (
    <PageSkeleton
      subtitle="Preparing the latest SafeKey verification workflow."
      title="Loading SafeKey"
    />
  );
}
