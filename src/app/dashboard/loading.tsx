import { PageSkeleton } from "@/components/page-skeleton";

export default function DashboardLoading() {
  return <PageSkeleton subtitle="Preparing cases, metrics, and presentation portfolio." title="Loading landlord workspace" />;
}
